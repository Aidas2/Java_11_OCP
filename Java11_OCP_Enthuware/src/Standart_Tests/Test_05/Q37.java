package Standart_Tests.Test_05;

import java.util.List;

public class Q37 {
    public static void main(String[] args) {

    }
}

//    Given:

    class Book {
        String isbn;
        String title;

        public Book(String isbn, String title) {
            this.isbn = isbn;
            this.title = title;
        }
        //accessors not shown
        // assume appropriate implementations of equals and hashCode based on isbn property }  and the following code snippet:
//        List<Book> books = getBooksByAuthor("Ludlum");
//        books.stream().sorted().forEach(b->System.out.println(b.getIsbn())); //1
        // Assuming that getBooksByAuthor is a valid method that returns a List of Books, which of the following statements is/are true?
    }
